<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 按下状态 -->
    <item android:drawable="@drawable/m_bottom_select_bg" android:state_pressed="true"/>

    <!-- 选中状态 -->
    <item android:drawable="@drawable/m_bottom_select_bg" android:state_selected="true"/>

    <!-- 默认状态 -->
    <item android:drawable="@android:color/transparent"/>

</selector>